Implement the Linear issue with ID: $ARGUMENTS

If no issue ID was provided, use the Linear MCP to list the top 5 open issues in the Heart Beater MC project (ordered by priority), display them as a numbered list with ID / Title / Priority, then ask the user which one to work on.

Once an issue ID is confirmed, invoke the `linear-implementer` sub-agent with that issue ID as the task. Pass the full issue ID exactly as given (e.g. MKA-07).
