Fetch all open issues in the Heart Beater MC project via the Linear MCP and display them as a table with columns: ID | Title | Status | Priority. Sort by priority (Urgent → High → Medium → Low), then by ID within each priority group.

After displaying the table, ask the user which issue they want to work on. Once they select one, invoke `/implement-linear-issue` with that issue ID.
